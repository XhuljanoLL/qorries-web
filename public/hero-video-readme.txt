Hero background video
=====================

Add your looping background clip to this folder as:

  hero-bg.mp4

The Hero component loads it from /hero-bg.mp4 (see HERO_VIDEO_SRC in src/components/Hero.jsx).
Use a short, compressed file for faster loads. No code changes are required if you keep this filename.
